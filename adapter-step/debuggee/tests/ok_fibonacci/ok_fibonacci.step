go 0, 10

proc go i, n
    less_than i, n
    if it
        fibonacci i, 0, 1
        log "F(", i, ") =", it

        add i, 1
        go it, n
    end
end

proc fibonacci n
    i = 2
    s = 0
    t = 1

    proc go
        result = null

        less_than i, n
        if it
            add i, 1
            i = it

            add s, t
            u = it
            s = t
            t = u
            go
        end
    end

    go 0, 0, 1
    it = t
end

proc less_than l, r
    js "$1 < $2", l, r
end

proc greater_than l, r
    js "$1 > $2", l, r
end
